import "./Header.css";

const Header: React.FC = () => {
  return (
    <header className="header">
      <img src="/logo-acme.jpg" alt="ACME logo" width={200} height={111} />
    </header>
  );
};

export default Header;
